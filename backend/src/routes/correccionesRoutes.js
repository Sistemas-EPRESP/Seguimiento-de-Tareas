const express = require('express');
const router = express.Router();
const correccionesController = require('../controllers/correccionesController');

// Rutas para correcciones
//router.post('/', correccionesController.createCorreccion); // Crear una corrección
//router.get('/', correccionesController.getAllCorrecciones); // Obtener todas las correcciones
//router.get('/:id', correccionesController.getCorreccionById); // Obtener una corrección por ID
//router.put('/:id', correccionesController.updateCorreccion); // Actualizar una corrección
//router.delete('/:id', correccionesController.deleteCorreccion); // Eliminar una corrección

module.exports = router;